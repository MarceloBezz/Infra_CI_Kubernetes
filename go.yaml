apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-api
  labels:
    nome: go
spec:
  selector:
    matchLabels:
      nome: go
  replicas: 3
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        nome: go
    spec:
    
      containers: 
       - name: go
         image: marcelobezz07/api-go-dev:28
         ports:
          - containerPort: 8000
         env:
           - name: DB_HOST
             valueFrom:
              secretKeyRef:
                name: dbhost
                key: HOST
           - name: DB_PORT
             valueFrom:
              secretKeyRef:
                name: dbport
                key: DBPORT
           - name: DB_USER
             valueFrom:
              secretKeyRef:
                name: dbuser
                key: USER
           - name: DB_PASSWORD
             valueFrom:
              secretKeyRef:
                name: dbpassword
                key: PASSWORD
           - name: DB_NAME
             valueFrom:
              secretKeyRef:
                name: dbname
                key: DBNAME
           - name: PORT
             valueFrom:
              secretKeyRef:
                name: port
                key: PORT
         livenessProbe:
           httpGet:
             path: /bruno
             port: 8000
           initialDelaySeconds: 60
           periodSeconds: 5
      restartPolicy: Always
